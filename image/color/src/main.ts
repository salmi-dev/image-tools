import { SDImageColorInfo } from '@salmidev/image-color';

if (import.meta.main) {
  console.log("Welcome to SalmiDev Image Color Library!");
  console.log(`     - version: ${SDImageColorInfo.version}`);
}
